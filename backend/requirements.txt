# PrivaSee backend — production dependencies
# Installed by Posit Connect on deployment. Keep this lean: no test tools here.

# --- Web framework ---
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.9       # multipart form uploads
aiofiles>=23.0.0              # required by StaticFiles (conditionally mounted)

# --- Data validation ---
pydantic>=2.5.0

# --- HTTP client (for Databricks Model Serving calls) ---
httpx>=0.26.0

# --- HTTP (UCSessionManager — Databricks Files REST API) ---
requests>=2.31.0

# --- Environment / configuration ---
python-dotenv>=1.0.0
