[pytest]
testpaths = tests
asyncio_mode = auto
addopts = --cov=app --cov-report=term-missing

markers =
    unit: pure unit tests with no I/O or external dependencies
    integration: integration tests using mocked external services (SessionManager, Databricks)
    contract: contract tests verifying mock responses conform to the Databricks endpoint schema
