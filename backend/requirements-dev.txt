# PrivaSee backend — development / test dependencies
# Install locally with: pip install -r requirements-dev.txt
# NOT used by Posit Connect.

-r requirements.txt

# --- Image processing (test fixtures) ---
Pillow>=10.2.0

# --- Testing ---
pytest>=7.4.0
pytest-asyncio>=0.21.0
pytest-cov>=4.1.0
jsonschema>=4.19.0
pymupdf>=1.23.0           # fitz — creates test PDFs with known text positions
