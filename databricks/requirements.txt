# ============================================================================
# Document Intelligence Model - Python Dependencies
# ============================================================================

# Core MLflow for model serving
mlflow>=2.10.0

# Data processing
pandas>=2.0.0

# PDF processing
PyMuPDF>=1.23.0  # fitz for PDF text extraction and rendering

# Word document processing
python-docx>=1.1.0

# Azure Document Intelligence (OCR)
azure-ai-documentintelligence>=1.0.0
azure-core>=1.29.0

# Vision AI providers
anthropic>=0.25.0  # Claude vision service
openai>=1.12.0     # Azure OpenAI vision service

# Async support (allows asyncio.run() inside Databricks Serverless which uses its own event loop)
nest-asyncio>=1.6.0

# Image processing (used by MaskingService for PNG/JPG inputs)
Pillow>=9.0.0

# Fake data generation (used by FakeDataService after entity extraction)
Faker>=22.0.0
python-dateutil>=2.9.0
gender-guesser>=0.4.0  # optional — graceful fallback if not installed

# Testing dependencies (development only)
pytest>=8.0.0
pytest-cov>=4.1.0
