# Dependencies for the Databricks model environment.
# These are installed into the MLflow model's Python environment and must be
# compatible with the Databricks Runtime version used for Model Serving.
#
# TODO: pin exact versions once the DBR version is confirmed.

# --- MLflow (provided by Databricks Runtime, listed for local dev) ---
mlflow>=2.10.0

# --- Anthropic Claude ---
anthropic>=0.20.0

# --- Azure Document Intelligence ---
azure-ai-documentintelligence>=1.0.0
azure-core>=1.30.0

# --- Image handling ---
Pillow>=10.2.0

# --- Data utilities ---
pydantic>=2.5.0
python-dateutil>=2.9.0

# --- Databricks SDK (for secret retrieval inside the model) ---
databricks-sdk>=0.20.0
